{
  "buildCommand": "rm -rf node_modules package-lock.json && npm install && npm run docs:build",
  "outputDirectory": "docs/.vitepress/dist",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "vitepress"
}